#pragma once

#include "version"

#if CTL_LIBCPP == CTL_LIBCPP_GLIBCXX && \
	CTL_COMPILER & CTL_COMPILER_FLAG_ZAPCC
namespace std
{
	class CTL_LOCAL_CLASS strong_ordering
	{
	};
}
#endif
#include <chrono>
