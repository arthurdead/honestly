#pragma once

#include "version"
#include <stack>

namespace ctl
{
	template <typename T, typename C = std::deque<T>>
	class CTL_LOCAL_CLASS stack : public std::stack<T, C>
	{
		using __base = std::stack<T, C>;

	public:
		void clear() noexcept;
	};
}

#include "__private/stack.tpp"
